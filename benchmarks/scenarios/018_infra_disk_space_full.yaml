scenario_id: "018_infra_disk_space_full"
category: INFRA
error_log_path: benchmarks/scenarios/fixtures/018_infra_disk_space_full.json
expected_classification: INFRA
expected_security_verdict: SAFE
expected_resolution_action: "Clean up or expand disk space"
ground_truth_notes: |
  An "[Errno 28] No space left on device" write failure is an infrastructure /
  resource-layer problem (disk capacity), not application code logic, so it is
  classified INFRA. Expected verdict SAFE: the fix is an operational cleanup or
  capacity-expansion recommendation; the fix artifact does not directly execute
  production config writes, database writes, or touch secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
