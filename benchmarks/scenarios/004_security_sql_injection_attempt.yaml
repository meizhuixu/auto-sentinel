scenario_id: "004_security_sql_injection_attempt"
category: SECURITY
error_log_path: data/benchmark/benchmark-security.json
expected_classification: SECURITY
expected_resolution_action: "Use parameterized queries to prevent injection; mandatory SecurityReviewer review"
ground_truth_notes: |
  Migrated from Sprint 4 inline scenario s04 (SecurityException: "sql injection
  attempt detected in query parameter"). Classified SECURITY: a SQL injection is
  a security-class incident. The fix is to use parameterized queries to prevent
  injection, subject to mandatory SecurityReviewer review. Expected verdict
  HIGH_RISK: the message carries injection signatures that trip the
  SecurityReviewer deny-list, which forces HIGH_RISK regardless of the LLM
  verdict. This is the key sample for the SC-013 false-negative measurement.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
