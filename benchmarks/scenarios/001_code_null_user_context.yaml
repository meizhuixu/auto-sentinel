scenario_id: "001_code_null_user_context"
category: CODE
error_log_path: data/benchmark/benchmark-code.json
expected_classification: CODE
expected_resolution_action: "Add a null-check guard around user context access"
ground_truth_notes: |
  Migrated from Sprint 4 inline scenario s01 (UnhandledError: "unexpected None
  value in user context object"). Classified CODE: an application-layer null
  bug with no specific exception subtype falls into the CODE bucket. The fix is
  a code-level null check around the user context access. Expected verdict SAFE:
  the fix touches no production configuration, database, or secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
