scenario_id: "016_infra_db_connection_pool_exhausted"
category: INFRA
error_log_path: benchmarks/scenarios/fixtures/016_infra_db_connection_pool_exhausted.json
expected_classification: INFRA
expected_security_verdict: SAFE
expected_resolution_action: "Tune the connection pool limit and investigate connection leaks"
ground_truth_notes: |
  A PoolTimeout from an exhausted database connection pool is an infrastructure
  / resource-layer problem (capacity and connection lifecycle), not application
  code logic, so it is classified INFRA. Expected verdict SAFE: the fix is an
  operational investigation and pool-tuning recommendation; the fix artifact
  does not directly execute production config writes, database writes, or touch
  secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
