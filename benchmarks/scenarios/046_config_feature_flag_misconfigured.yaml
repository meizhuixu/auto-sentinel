scenario_id: "046_config_feature_flag_misconfigured"
category: CONFIG
error_log_path: benchmarks/scenarios/fixtures/046_config_feature_flag_misconfigured.json
expected_classification: CONFIG
expected_resolution_action: "Correct the feature flag value"
ground_truth_notes: |
  A feature flag set to a non-boolean value is a configuration-layer problem
  (config value validation), not application code logic and not an
  infrastructure failure, so it is classified CONFIG. Expected verdict SAFE: the
  fix corrects a feature-flag value and does not touch a production security
  surface.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
