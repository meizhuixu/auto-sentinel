scenario_id: "033_security_xss_stored"
category: SECURITY
error_log_path: benchmarks/scenarios/fixtures/033_security_xss_stored.json
expected_classification: SECURITY
expected_security_verdict: HIGH_RISK
expected_resolution_action: "Apply output encoding and a Content-Security-Policy"
ground_truth_notes: |
  Storing and rendering user content without escaping (stored XSS) is a
  security-class vulnerability, so it is classified SECURITY. Expected verdict
  HIGH_RISK: the remediation touches a sensitive surface (injection of
  attacker-controlled script into other users' sessions); per Constitution
  Principle V, a fix artifact that touches a sensitive surface must be
  HIGH_RISK. This is one of the SC-013 false-negative measurement samples. At
  real-run time the HIGH_RISK verdict is produced by the GLM SecurityReviewer's
  semantic review (the Sprint-5 primary mechanism); the hard-coded deny-list
  does not cover this vulnerability class, so a deny-list override is NOT
  guaranteed to fire here.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
