scenario_id: "024_infra_network_partition"
category: INFRA
error_log_path: benchmarks/scenarios/fixtures/024_infra_network_partition.json
expected_classification: INFRA
expected_security_verdict: SAFE
expected_resolution_action: "Investigate the network and wait for partition recovery"
ground_truth_notes: |
  A loss of quorum because peer nodes are unreachable across a network
  partition is an infrastructure / network-layer problem, not application code
  logic, so it is classified INFRA. Expected verdict SAFE: the fix is a network
  investigation and recovery-wait recommendation; the fix artifact does not
  directly execute production config writes, database writes, or touch secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
