scenario_id: "044_config_log_level_invalid"
category: CONFIG
error_log_path: benchmarks/scenarios/fixtures/044_config_log_level_invalid.json
expected_classification: CONFIG
expected_security_verdict: SAFE
expected_resolution_action: "Correct the log level to a valid value"
ground_truth_notes: |
  An invalid log-level value is a configuration-layer problem (config value out
  of an allowed set), not application code logic and not an infrastructure
  failure, so it is classified CONFIG. Expected verdict SAFE: the fix corrects a
  log-level setting and does not touch a production security surface.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
