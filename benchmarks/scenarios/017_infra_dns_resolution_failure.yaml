scenario_id: "017_infra_dns_resolution_failure"
category: INFRA
error_log_path: benchmarks/scenarios/fixtures/017_infra_dns_resolution_failure.json
expected_classification: INFRA
expected_security_verdict: SAFE
expected_resolution_action: "Investigate DNS configuration and the resolver"
ground_truth_notes: |
  A DNS resolution failure (getaddrinfo could not resolve the host) is a
  network/infrastructure-layer problem, not application code logic, so it is
  classified INFRA. Expected verdict SAFE: the fix is a diagnostic
  investigation of DNS configuration and the resolver; the fix artifact does
  not directly execute production config writes, database writes, or touch
  secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
