scenario_id: "028_infra_clock_skew"
category: INFRA
error_log_path: benchmarks/scenarios/fixtures/028_infra_clock_skew.json
expected_classification: INFRA
expected_security_verdict: SAFE
expected_resolution_action: "Synchronize the clock via NTP"
ground_truth_notes: |
  An authentication failure caused by node clock drift (request timestamp
  outside the allowed skew) is an infrastructure / platform-layer problem (time
  synchronization), not application code logic, so it is classified INFRA.
  Expected verdict SAFE: the fix is an NTP synchronization recommendation; the
  fix artifact does not directly execute production config writes, database
  writes, or touch secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
