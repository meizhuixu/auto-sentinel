scenario_id: "003_config_jwt_secret_missing"
category: CONFIG
error_log_path: data/benchmark/benchmark-config.json
expected_classification: CONFIG
expected_resolution_action: "Provide the missing JWT_SECRET_KEY environment variable"
ground_truth_notes: |
  Migrated from Sprint 4 inline scenario s03 (ConfigurationError: "required
  environment variable JWT_SECRET_KEY is not set"). Classified CONFIG: a missing
  required environment variable is a configuration problem. The fix is to supply
  the missing JWT_SECRET_KEY environment variable. Expected verdict SAFE:
  supplying an environment variable is not a change to sensitive production
  configuration.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
