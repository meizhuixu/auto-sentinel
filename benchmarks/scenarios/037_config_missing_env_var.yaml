scenario_id: "037_config_missing_env_var"
category: CONFIG
error_log_path: benchmarks/scenarios/fixtures/037_config_missing_env_var.json
expected_classification: CONFIG
expected_resolution_action: "Provide the missing environment variable"
ground_truth_notes: |
  A missing required (non-secret) environment variable is a configuration-layer
  problem (environment configuration), not application code logic and not an
  infrastructure failure, so it is classified CONFIG. Expected verdict SAFE:
  the fix simply supplies the missing tuning variable and does not touch a
  production security surface (same reasoning as the migrated s03).
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
