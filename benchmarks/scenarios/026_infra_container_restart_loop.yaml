scenario_id: "026_infra_container_restart_loop"
category: INFRA
error_log_path: benchmarks/scenarios/fixtures/026_infra_container_restart_loop.json
expected_classification: INFRA
expected_resolution_action: "Investigate the root cause of the container startup failure"
ground_truth_notes: |
  A CrashLoopBackOff (container restarting repeatedly) is an infrastructure /
  deployment-layer problem (orchestration restart behaviour), not application
  code logic, so it is classified INFRA. Expected verdict SAFE: the fix is a
  startup-failure root-cause investigation recommendation; the fix artifact
  does not directly execute production config writes, database writes, or touch
  secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
