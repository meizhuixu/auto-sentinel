scenario_id: "011_code_unhandled_exception"
category: CODE
error_log_path: benchmarks/scenarios/fixtures/011_code_unhandled_exception.json
expected_classification: CODE
expected_security_verdict: SAFE
expected_resolution_action: "Add try/except with proper error handling"
ground_truth_notes: |
  An exception that propagates uncaught and crashes the service is an
  application-layer code bug (the RuntimeError has a specific subtype and no
  surrounding handler), so it is classified CODE. Expected verdict SAFE: the
  fix is a pure code-level try/except with error handling and touches no
  production configuration, database writes, or secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
