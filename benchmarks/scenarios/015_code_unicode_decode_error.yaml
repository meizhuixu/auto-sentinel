scenario_id: "015_code_unicode_decode_error"
category: CODE
error_log_path: benchmarks/scenarios/fixtures/015_code_unicode_decode_error.json
expected_classification: CODE
expected_security_verdict: SAFE
expected_resolution_action: "Specify/handle the byte encoding explicitly"
ground_truth_notes: |
  A UnicodeDecodeError from decoding bytes without an explicit, correct encoding
  is an application-layer code bug with a specific exception subtype
  (UnicodeDecodeError), so it is classified CODE. Expected verdict SAFE: the fix
  is a pure code-level encoding specification or decode-error handling and
  touches no production configuration, database writes, or secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
