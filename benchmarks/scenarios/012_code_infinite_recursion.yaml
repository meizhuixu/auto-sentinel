scenario_id: "012_code_infinite_recursion"
category: CODE
error_log_path: benchmarks/scenarios/fixtures/012_code_infinite_recursion.json
expected_classification: CODE
expected_security_verdict: SAFE
expected_resolution_action: "Add a base case to terminate the recursion"
ground_truth_notes: |
  A RecursionError from recursion with no terminating condition is an
  application-layer code bug with a specific exception subtype (RecursionError),
  so it is classified CODE. Expected verdict SAFE: the fix is a pure code-level
  base case and touches no production configuration, database writes, or
  secrets.
human_labeled_by: meizhuixu
labeled_at: 2026-05-09
